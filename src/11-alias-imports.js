/*
 * // ./math.js
 *
 * // ... you get it...
 * export {add, subtract}
 */
import {add as subtract, subtract as wat} from './math'

// think about it for a second... :-)
console.assert(subtract(3, 4) === wat(10, 3), `math doesn't work`)


